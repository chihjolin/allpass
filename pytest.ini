[pytest]
pythonpath = 
    backendf
    aiservices
addopts = -ra -q -v -s
# 自動識別 async def 測試函式
asyncio_mode = auto
# 設定 fixture 的 loop scope，避免 scope mismatch 錯誤
asyncio_default_fixture_loop_scope = session