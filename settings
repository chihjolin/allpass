#postgis
# Host（本機）連線用 port，給 docker-compose 的 port 映射用
DB_HOST_PORT=15432
 
# DB 設定（容器內用）
DB_PORT=5432
DB_HOST=postgis
DB_NAME=allpass_db
DB_USER=allpass_user
DB_PASSWORD=allpass

#backend
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=TRUE
#FLASK_DB_CONNECTION_RETRY=5

#frontend
WEB_PORT=80